# PyKanban - Kanban Board App

A **Python desktop Kanban application** where:

- Each task is a **single Markdown file**
- Markdown files are the **database (source of truth)**
- YAML frontmatter stores structured task metadata
- The GUI is a **projection layer over the filesystem**
- No SQLite, no JSON database, no hidden state
